@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<Client>

<div class="create-client-button">
    <a class="btn btn-primary" href="@Url.Action("CreateClient", "Client")">Create Client</a>
</div>

<div class="admin-button">
    <a class="btn btn-primary" href="@Url.Action("AdminPage", "Account")">Admin Page</a>
</div>

@if (Model.Any())
{
        <table class="table table-striped table-hover">
            <thead>
                <tr class="properties">
                    <th>Id</th>
                    <th> Name</th> 
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Address</th>
                    <th>City</th>
                    <th>State</th>
                    <th>Country</th>

                </tr>
            </thead>
            <tbody>
            @foreach (var client in Model)
            {
                        <tr>
                            <td>@client.Client_Id</td>
                            <td>@client.FirstName</td>
                            <td>@client.Email</td>
                            <td>@client.Phone</td>
                            <td>@client.Address</td>
                            <td>@client.City</td>
                            <td>@client.State</td>
                            <td>@client.Country</td>
                            <td>
                                <a class="btn btn-sm btn-primary" href="@Url.Action("EditClient", "Client", new { id = client.Client_Id })">Edit</a>
                            </td>
                            <td>

                                <a class="btn btn-primary" href="@Url.Action("DeleteClient", "Client", new { ClientId =client.Client_Id})">Delete</a>

                            </td>
                        </tr>
            }
            </tbody>
        </table>
}
else
{
        <p>No clients found.</p>
}
<style>
    .create-client-button {
        position: absolute;
        right: 0;
        top: 40px;
        padding: 15px 25px;
        font-size: 16px;
    }

    .admin-button {
        position: absolute;
        left: 0;
        top: 40px;
        padding: 10px 20px;
        font-size: 15px;
        color: #fff;
    }

    .btn-lg {
        font-size: 15px;
        padding: 10px 20px;
    }

    .btn-primary {
        font-size: 16px;
        padding: 10px 20px;
        color: #fff;
        background-color: #007bff;
        border-color: #007bff;
    }

    .btn-danger {
        font-size: 16px;
        padding: 10px 20px;
        color: #fff;
        background-color: #0069d9;
        border-color: #0069d9;
    }

    .btn-primary:hover,
    .btn-primary:focus,
    .btn-primary:active {
        color: #fff;
        background-color: #0069d9;
        border-color: #0062cc;
    }

    .btn-danger:hover,
    .btn-danger:focus,
    .btn-danger:active {
        color: #fff;
        background-color: #0069d9;
        border-color: #0069d9;
    }

    table {
        border-collapse:; 
        width: 100%;
    }

    th, td {
        padding: 8px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    .delete-button {
        margin-left: 5px;
    }

    .properties {

        color: black ; 
    }

        .properties th {
            font-weight: bold;
        }

    .table-hover tbody tr:hover td {
        background-color: #f5f5f5;
    }
    </style>